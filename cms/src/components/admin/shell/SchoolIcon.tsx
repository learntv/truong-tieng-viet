import React from 'react'

import { SchoolMark } from './SchoolMark'

/**
 * `admin.components.graphics.Icon` — the mark alone, for the small slots the framework fills
 * itself (the sign-in card, and `AppHeader`'s icon).
 */
export const SchoolIcon: React.FC = () => <SchoolMark variant="icon" />
