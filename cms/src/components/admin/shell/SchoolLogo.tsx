import React from 'react'

import { SchoolMark } from './SchoolMark'

/** `admin.components.graphics.Logo` — mark and name, for the framework's full-size slots. */
export const SchoolLogo: React.FC = () => <SchoolMark />
